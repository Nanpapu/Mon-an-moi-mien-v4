import { lightColors } from '../colors';
import { ThemeType } from './types';

export const lightTheme: ThemeType = {
  id: 'light',
  name: 'Chế độ sáng',
  icon: 'sunny-outline',
  colors: lightColors,
};
